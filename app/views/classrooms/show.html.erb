<nav class="navbar navbar-default">
  <div class="container-fluid">
      <a class="navbar-brand"> <h3 class="text-info">Student Behavior Tracker</h3></a>
      <ul class="nav navbar-nav navbar-right">
              <h4 class="btn-group"><%= link_to "Return to all Classrooms", classrooms_path, class: "btn btn-success" %></h4>
            </ul>
  </div>
</nav>

<div class="jumbotron">
  <div class="container-fluid">
    <h1>Classroom: <%= @classroom.teacher %></h1>
  </div>
</div><br><br>


<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th class="text-info">#</th>
      <th class="text-info">Student Name</th>
    </tr>
  </thead>
  <tbody>
    <% @classroom.students.each_with_index do |student, index|  %>
      <tr>
        <td><%= index + 1 %></td>

    <td><%= link_to student.name, classroom_student_path(student) %></td>

    <% end %>
  </tr>
  </tbody>
  </table>
