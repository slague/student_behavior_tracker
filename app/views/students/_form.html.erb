<%= form_for [@observation] do |f| %>

<%= f.label :behavior_id, 'Behavior' %>
<%= f.collection_select(:behavior_id, Behavior.all, :id, :name, :include_blank => "Other behavior") %>

<!-- Is this a target behavior? Select from one of the student_target_behaviors -->
<!-- If not a target behavior? Select from one of the behaviors -->
<!-- Either way, (target behavior or other behvaior) complete the rest of the observatio form-->

<%= f.label :behavior_id, 'Behavior' %>
<%= f.collection_select(:behavior_id, Behavior.all, :id, :name, :include_blank => "Other behavior") %>

<%= f.label :date %>
<%= f.select_date :date %>

<%= f.label :time %>
<!-- f.time :time %> -->

<%= f.label :duration %>
<%= f.text_field :duration %>

<%= f.label :location %>
<%= f.text_field :location %>

<%= f.label :logged_by %>
<%= f.text_field :logged_by %>

<%= f.label :comments %>
<%= f.text_area :comments %>

<%= f.submit %>
<% end %>
